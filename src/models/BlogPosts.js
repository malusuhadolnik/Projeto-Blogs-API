// um blog post pertence a um usuário = belongs to models.User,
// foreignKey é a mesma , as 'users' (que é nome da tabela)

// um blog post pertence a uma unica categoria = belongs to models.Category ou models.PostCategories???
